# Storkvid 🪽

Инструмент для обработки видео.

## Требования

- Go 1.23.4 или выше
- FFmpeg (включая ffprobe) установлен в системе и доступен в PATH

### Установка FFmpeg

**macOS:**

```bash
brew install ffmpeg
```

**Linux (Ubuntu/Debian):**

```bash
sudo apt update
sudo apt install ffmpeg
```

## Установка

```bash
go install github.com/Danoxv/storkvid@latest
```

Или клонируйте репозиторий и соберите локально:

```bash
git clone https://github.com/Danoxv/storkvid.git
cd storkvid
go build -o storkvid
```

## Использование

```bash
storkvid [флаги] <путь_к_видео>
```

### Флаги

- `--crf <число>` - Constant Rate Factor (качество видео)
    - Значение по умолчанию: `23`
    - Диапазон: `0-51` (меньше = лучше качество, больше файл)

- `--no-audio` - Выключить аудио в выходном файле

## Автор

https://github.com/Danoxv
